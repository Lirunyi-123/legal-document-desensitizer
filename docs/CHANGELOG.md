# 更新日志 (CHANGELOG)

本文件收录《法律文书脱敏工具》各版本的更新说明（从 SKILL.md 迁移而来）。

---

## v5.3 缺陷修复与一致性升级（2026-08-13）

- **docx 表格结构修复**：读取与写回统一为"逐单元格段落"展平，修复含表格
  文书脱敏/还原后内容错位、丢失、混入 ` | ` 的问题。
- **xlsx 元数据清理生效**：清理逻辑移到 `save()` 之前，输出不再残留
  `creator/lastModifiedBy/title` 等敏感元数据。
- **`decrypt` 安全修复**：`-k` 参数改用 `ast.literal_eval` 解析字节字面量，
  移除 `eval` 注入风险。
- **scan 与 mask 对齐**：`scan` 复用 `_extract_scan_value`（人名只取姓名、
  公司名先修剪、地址只取本体）；补齐罚没许可证号/地块编号/银行机构规则；
  独立微信号要求含数字/下划线；车牌号补词边界，与脱敏行为一致。
- **`full` 命令修复**：Ollama 请求去掉 `format:'json'`，避免与
  `### 脱敏后文本` 混合格式冲突导致解析失败。
- **skill 工具声明**：`allowed-tools` 增加 `bash`，支持代为执行本机命令。
- 新增 9 项回归测试（全量 206 项通过，红队 105 项命中、误报 0）。

## v5.3 安全加固续篇（2026-08-14）

按上轮审查结论，把"默认只允许本地"从文档承诺升级为代码强制（Fail-Closed）：

- **本地性默认 Fail-Closed**（`_check_offline` 改名 `_check_endpoint_policy`）：
  `full` 命令与 `mask --ner-backend llm` 的端点非本机时默认直接中止，须显式加
  `--allow-remote-llm` 才放行（打印警告：规则层处理后的文本将被发送至该端点）；
  `--offline` 老语义不变（禁一切非 ollama API + 禁非本机端点），且与
  `--allow-remote-llm` 互斥（同时给出时中止并报错）。
- **审计单留痕**：单文件与批量审计单 JSON 新增 `allow_remote_llm` 与
  实际 `llm_endpoint` 字段。
- **IPv6 端点判断修复**：`urlparse('http://[::1]:11434').hostname` 返回
  `::1`（无方括号），统一剥离方括号后再比较，localhost / 127.0.0.1 / ::1
  白名单不变。
- **LLM 合并映射修复**：LLM 条目 `count` 改取规则层文本中 original 的出现次数
  （修复规则层与 LLM 层共用占位符时 count 被放大）；`reorder_merged_mapping`
  改为逐次推进扫描，同一原始值多次出现时取第 n 次位置，"一行 = 一次出现"
  的配对语义不再退化。
- **`parse_full_response` 贪婪正则修复**：补充映射表 JSON 数组只在
  `### 补充映射表` 标记之后搜索，避免"脱敏后文本"小节内的 `[{...}]` 形态
  跨小节错配导致解析失败。
- **密码文件隔离**：非交互自动生成的映射表密码默认写入
  `~/.desensitizer/keys/<映射表文件名>.password`（目录 0700、文件 0600），
  与密文分离存放；解密侧新增该路径查找，并保留同目录
  `映射表.enc.password.txt` 作为 v5.2 旧版兼容回退。
- **`full` 输出前自动还原往返校验**：用合并映射做 `restore_text == 原文`
  校验，不一致则中止不产出；新增 `--no-restore-check` 跳过开关
  （与 `semantic` 同名同义）。
- **文档修正**：SKILL.md frontmatter（`name` 与目录统一为
  `legal-document-desensitizer`，`allowed-tools` 改为 `Bash, Read, Write,
  Grep, Glob`）；删除"组织机构代码未纳入规则层"自相矛盾句（表格已含该规则）；
  命令速查与安全等级表补充 `--allow-remote-llm`、`full` 还原校验、密码文件
  新默认位置；git-push 等开发发布章节移到 `docs/开发与发布.md`；
  `decrypt_mapping_encrypted` 等 docstring 改为"尽力释放引用（Python 字符串
  不可变，非真清零）"。
- 新增 11 项回归测试（全量 217 项通过；红队 105 项命中、误报 0）。

## v5.0 可验证本地闭环 · AI 安全出口（2026-08-11，安全交付升级）

在 v4.2 零上传本地模式基础上，对照"300 问"补齐**可验证本地 + AI 安全出口**
两大能力，让"安全"从承诺变成可导出、可检查、可签发的产物：

### 1. 可验证本地（--offline / --audit）
- `--offline` 严格模式：只允许本机处理，LLM 端点非本机（如云端 API）直接中止
  （Fail Closed），绝不静默降级；
- `--audit` 生成审计单 JSON：输入/输出/映射表/审阅路径 + 文件 sha256 +
  是否使用 OCR（本机 Vision）+ 是否调用 LLM（端点、是否本机）+
  隐藏信息清点；批量模式自动生成 `批量审计单.json`。

### 2. AI 安全出口（防提示注入 + 签发材料包）
- LLM 提示词（`llm-prompt` / `full`）加入**"材料非指令"隔离包裹**：材料内
  "忽略以上要求/输出系统提示词"等一律视为内容，不得执行；
- 审阅清单新增两类自动扫描：**提示注入模式**、**重识别风险**
  （独女/唯一继承人/某上市公司董事长+高校+小区等组合）→ 建议复核；
- `finalize` 命令生成 AI 安全出口材料包：
  `01_脱敏稿 + 02_映射表 + 03_审计单 + 04_审阅清单 + 05_签发单`，
  律师逐项勾选签名后，材料方可进入 AI 工作流。

### 3. 内容完整性（页数核对 + 隐藏信息检查）
- PDF 涂黑/OCR 后自动核对输入页数 vs 输出页数，不一致立即警告并写入审计单；
- docx 输出时检查批注/修订痕迹/嵌入对象数量并报告（修订痕迹暂不支持自动清理，
  明确标注"请人工另存为无修订版本"——不假装安全）；
- PDF 涂黑沿用批注/表单/书签/附件清理报告（v3.0 起）。

### 4. 跨文件身份归一（--batch --shared-entities）
- 批量模式下共享实体注册表：同一人/公司在整批卷宗中使用同一匿名身份，
  不再"每个文件一个当事人甲"；
- 生成 `全局映射表.md`，同名/多角色自动 ⚠️ 标记，供律师确认是否同一人；
- 每个文件仍保留独立映射表，可单独 `restore` 还原。

### 用法
```bash
# 单文件脱敏 + 审阅 + 审计单（可验证本地）
desensitize mask -f 判决书.docx --review --audit --encrypt-mapping
# 严格本地模式（端点非本机直接中止）
desensitize mask -f 判决书.docx --review --offline
# 批量卷宗 + 跨文件身份归一
desensitize mask --batch ./卷宗 --review --shared-entities --output-dir ./脱敏输出
# 律师签发：AI 安全出口材料包
desensitize finalize -f 判决书_desensitized.docx -m 映射表.enc \
  --audit 判决书_desensitized_审计单.json --original 判决书.docx -o 材料包
```

### 验证方法（v5.0 起）
```bash
python3 -m unittest test_desensitize     # 181 个单测（v4.2 172 + v5.0 9）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
```

## v5.1 实战修复（2026-08-12，众楽汇扫描件合同脱敏）

基于一份 2 页扫描件销售合同（`众楽汇5.pdf`）的实战脱敏，修复 3 类问题并
新增 3 项回归测试（全量 181 → 184 项通过）：

### 1. 软链启动失效（~/.local/bin/desensitize 报"内置 OCR 不可用"）
- 根因：直接执行软链时 `__file__` 指向软链本身、`sys.path[0]` 指向软链目录，
  工具据此找 `ocr_vision.swift`/`image_redact.py` 全部落空 → OCR 静默不可用；
- 修复：脚本启动时用 `os.path.realpath(__file__)` 定位真实目录并注入
  `sys.path`，资源文件路径全部改用 realpath；
- 回归：`TestLauncherRobustness`（软链方式跑 `mask` 断言输出正常）。

### 2. Swift 编译器与 SDK 版本不匹配 → OCR 重编译失败即"全废"
- 根因：Xcode Command Line Tools 更新后 `swiftc`（6.3.3）与 SDK（6.3.2）
  版本不匹配，`_ensure_ocr_bin` 删除旧缓存重编译失败 → 直接返回不可用；
- 修复：重编译失败时**不静默放弃**——先回退沿用本机已有缓存二进制
  （过自检即用），再到 `/tmp`、`/private/tmp` 等常见位置寻找可用二进制并
  复制到当前 TMPDIR；同时给带坐标 OCR（`ocr_vision_boxes`）补上与文本 OCR
  一致的"自检 + 回退"策略（此前只查存在性，不校验可用性）；
- 均输出 stderr 提示（含修复建议：更新 Xcode Command Line Tools），不静默降级。

### 3. 扫描件漏涂 + 审阅清单还原校验误报
- 漏涂：OCR 同行多框重建时两家公司名被拼成一个带空格的串
  （`A公司 B公司`），整串定位单个框失败 → 该处不涂黑；
  修复：新增 `match_boxes_for` 定位函数，整串失败时按空白拆 token 逐个
  定位（宁多勿漏），涂黑数 19 → 21；
- 误报：扫描件流程 `redact_scanned_pdf` 用第二份 OCR 文本在内部重跑规则层，
  覆盖 `d._original_text`，而审阅清单仍拿它做还原往返校验 → 与第一份
  映射表不一致误报"❌ 还原往返校验失败"；
  修复：审阅校验改用 `_run_mask_file` 传入的第一份原文；
- 回归：`TestV39ImageRedact.test_match_boxes_token_fallback`、
  `TestReviewOriginalText`。

### 验证方法（v5.1 起）
```bash
python3 -m unittest test_desensitize     # 184 个单测
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
```

## v5.2 隐私默认升级（2026-08-12，AI 代跑场景防"原值进对话"）

针对"AI 在用户本机代跑脱敏命令"的场景，把默认行为改为**不向终端/对话暴露
敏感原值与密码**（全量单测 184 → 186 项通过）：

### 1. 警告不再打印敏感原值
- 未定位坐标/未涂黑/OCR 补全猜测等警告，默认只打印**数量**，原值写入本地
  `*_警告明细.txt`（0600 权限），避免"一行 OCR 原值打进终端输出"这类泄漏；
- 需要立即看到原值（仅限本人终端）时显式加 `--show-raw-warnings`。

### 2. 映射表密码默认落盘、不进终端
- `--encrypt-mapping` 在非交互环境自动生成随机密码并写入本地 0600 密码文件
  （默认 `映射表.password.txt`，可用 `--mapping-password-file` 指定），
  终端只打印文件路径，**密码本身绝不打印**；
- `decrypt` / `restore` / `semantic` / `finalize` 新增 `--password-file`，
  可从该本地文件读取密码还原，无需把密码贴在对话里。

### 3. Agent 代跑约定
- 只汇报"输出路径 + 审阅清单摘要 + 关键信息校验结果"，**不推送脱敏稿预览图
  进对话、不读取密码文件与警告明细内容**；
- 预览图、密码文件、明细文件全部留在本机输出目录，由律师自行查看。

### 4. 一键交付包（--pack / finalize 增强）
- `mask ... --pack`：脱敏完成后自动把全部交付物装进一个文件夹
  （`01_脱敏稿 / 02_映射表 / 03_审计单 / 04_审阅清单 / 05_签发单 /
  06_掩码文本`），并自动做还原往返校验；可用 `--pack-output` 指定目录、
  `--pack-original` 指定原件（默认取输入文件）；
- `finalize` 新增 `06_掩码文本`：扫描件/图片涂黑 PDF 无法反向 OCR，
  还原往返校验优先用与映射表同源的侧车，不再误报；
- 密码文件刻意**不进交付包**（防止整个文件夹被分享时密码外泄），
  它保留在交付包旁边，仅限本机使用。

### 验证方法（v5.2 起）
```bash
python3 -m unittest test_desensitize     # 188 个单测
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
```

## v4.2 零上传本地模式（2026-08-11，默认工作流）

针对"脱敏时把文书上传给 AI、原文可能被读取"的顾虑，从 v4.2 起默认工作流
改为**本地终端闭环**：

- **本 skill 不再接收原文**：只指导用户在本机终端执行脱敏命令，原文
  （含扫描件、OCR 文本、明文映射表）绝不进入 AI 对话。
- **一次性安装**：把 `desensitize.py` 软链为全局命令 `desensitize`
  （见下方"零上传本地模式"），依赖仅 `jieba` 必装，其余按需。
- **默认流程**：`desensitize mask -f 文书.docx --review --encrypt-mapping`
  全在本机执行；语义层缺口默认走本地 Ollama（数据不出本机）。
- **上传边界**：只有"脱敏稿 + 审阅清单"可以考虑交给云端 AI；原文与映射表
  永不进入 AI 对话。
- **同步修复**：`decrypt` 命令此前会先把二进制加密映射表当文本预读而崩溃，
  已修复（新增 2 项回归测试，全量 181 项，OCR 类用例需非沙箱环境）。

## v4.1 实战修复（2026-08-11 archive3 批量，34 份真实/合成文书）

基于 34 份公开判决书/裁定书/调解书/起诉状/银行流水（27 份 PDF、8 份扫描件）
的批量实战，修复 8 项问题，新增 8 项回归测试（全量 170 项通过）：

### 1. 扫描件 PDF 自动路由
- `mask -f 扫描件.pdf -o out.pdf` 现在**自动检测文本层**并走"原图涂黑"
  分支（此前需显式 `--image-redact`，否则报"文本层找不到待涂值"）。

### 2. 掩码文本侧车（PDF 语义层/还原校验基石）
- `mask --pdf-redact` 与扫描件涂黑均额外保存 `*_掩码文本.txt`：
  - 文本层 PDF：与映射表同源的脱敏文本（涂黑 PDF 文本层重排后无法
    逐字节对齐，侧车保证阶段二与还原校验精确）；
  - 扫描件：保存 `result.text`（此前 `image_redact._redact_pages`
    误返回原始 OCR 文本，侧车无占位符）。
- `run_semantic_pass` 先归一化折行占位符（`[当事人_5\n]` → `[当事人_5]`），
  映射配对失败时输出明确错误而非 `IndexError`。

### 3. 人名假阳性规则收紧
- 姓氏与前一字构成常见词（用**于**威某、相**关**底盘、**关**于威某、
  **有**关底盘）→ 拒绝；回看跨空白（"相\n关底盘"）同样拦截。
- 技术/机构名词尾缀/前缀（底盘/技术/关系/零部件/人员/图纸/信息/项目…）
  → 拒绝。
- 3 字候选因列末空白非法时继续尝试 2 字候选（修复"张伟 "漏掉"张伟"）。
- 黑名单补充：关于/相关/用于/关系/项目名/车底盘/关底盘等。

### 4. 英文文档不再被微信号规则误伤
- 中文字符占比 <2% 的文档禁用"独立微信号"启发式；
- 独立微信号候选必须含数字/下划线（纯字母的英文词 Banking/Limited/
  Statement 不再被当成微信号；纯字母微信号走"微信号：xxx"上下文规则）。

### 5. 银行流水交易行孤立姓名
- 新增 `_in_txn_row`：行首含日期、候选后紧跟 8 位以上数字账号
  （或候选前为转账/代发/消费等摘要词）→ 视为对方户名，即使单次出现
  也掩码（修复王秀英/张伟/陈志强漏脱敏）。
- 公司名修剪词表补充银行摘要词，修复"代发工资 杭州云杉科技有限公司"
  整串被当公司名的问题（摘要保留、公司名掩码）。

### 6. OCR 环境稳健性（macOS Vision）
- `ocr_vision.swift` 改用 `VNImageRequestHandler(url:)` 优先加载，
  cgImage 路径失败时回退（修复部分扫描件 `nilError` 静默为空）。
- `_ensure_ocr_bin`：独立 clang 模块缓存 + 源码更新自动重编 +
  `--selftest`（PIL+系统字体 "ABC 123" 校验输出），防止复用损坏的
  共享模块缓存导致"静默空"二进制。
- 注意：macOS Vision 在**沙箱内不可用**（返回 `nilError`），OCR 类
  任务需在非沙箱/本机环境运行。

### 7. 模板类文书空映射
- 无敏感信息的官方模板（如起诉状示范文本）阶段一映射表为空属正常，
  阶段二跳过并出说明，不再报错。


---

## v2.5 实战修复（2026-08 真实判决书）

根据对一份 42 页民事判决书（OCR 扫描版）的实战脱敏，修复/增强如下：

### 1. 还原（restore）精确配对
- 映射表改为 **"每处替换一行"**（同一原始值出现 N 次 → N 行，count=1），
  restore 按占位符在最终文本中的出现顺序逐一配对。
- `restore_text` 同时兼容旧格式（count>1 的行按 count 展开后再配对）。
- 映射表 Markdown 单元格**不再填充空格**，带首尾空格的原始值（如
  `349976351 元 `，OCR 常见"数字 元 ，"）可逐字节还原。
- 替换事件记录"当时文本位置"，用片段表回放换算最终位置；规则 pass 顺序
  与原文顺序不一致（金额多种写法分 pass）也能精确配对。

### 2. 角色词后名词不再误当人名
- 新增姓名形态门控：候选须以常见姓氏开头（或 jieba 整词分词），且不在
  "名词/动词词组"黑名单中。
- 修复实战误伤：`原告提供担保`、`法定代表人处签名`、`法定代表人印章`、
  `被告私章`、`原告主张驳回` 等不再被替换为人名。
- 支持"姓名+尾部动词"回退：`吴琳陆续`→`吴琳`+`陆续`、`金进跃与`→`金进跃`+`与`。
- 新增角色词：案外人、证人、担保人、出借人、借款人、收款人、付款人、发包人、
  承包人、分包人、代建人、工作人员、物业人员、项目经理、财务人员、会计、出纳等。

### 3. 公司名不再吞上下文
- 修剪匹配串前被误吞的上下文词（`原告金进跃与被告浙江华临建设集团有限公司`
  → 只替换公司名本身，保留`原告[当事人甲（原告）]与被告[合同乙方]`）。
- 公司规则跳过落在 `[占位符]` 内部的匹配，避免"简称套娃"（`[[第三方公司]]`）。
- 简称支持 2 字起（`华临公司`/`方汇公司`）及 OCR 行内空格（`华 临公司`）。

### 4. 金额与 OCR 空格
- 金额允许数字与单位间空格（`349976351 元`、`32000万 元整`）。
- 新增 7 位以上无单位大数规则（结算算式 `392485911.675`）。
- 中文大写金额规则不再把单字"陆"（陆续）误当金额。
- 公司/金额规则只容忍行内空格，不跨换行，保证 docx 逐段对齐。

### 5. 验证方法（每次改规则后必跑）
```bash
python3 -m unittest test_desensitize     # 59 个单测（含还原往返/审阅清单）
python3 evaluate.py                      # 60 个红队用例，含新增实战负样本
python3 desensitize.py mask -f 判决书.docx --save-mapping 映射表.md
python3 desensitize.py restore -f 判决书_desensitized.docx -m 映射表.md -o 还原.docx
# 还原后与原文逐段比对，要求 0 差异
```

## v2.6 两阶段工作流（v4.2 起默认本地终端执行）

- 阶段一（规则层，必做，本机执行）：`desensitize mask --review` 生成
  "规则层结果 + 审阅清单"，关键信息（身份证/手机/银行卡/案号/信用代码/
  执业证号等）必须清零；**原文绝不进入 AI 对话**。
- 律师审阅清单后，如认为有必要做语义层：
  - 默认：本机 Ollama（`desensitize full --llm-api ollama`），数据不出本机；
  - 或：只把**脱敏稿**交给云端 AI 做语义层（原文、映射表仍不得上传），
    且必须接受"法院名/案情细节可能被云端读取"这一残留风险。
- 语义层只处理低优先级信息：法院名称、项目名称、案情敏感细节、
  裸公司简称、OCR 空格/变体写法的人名公司名。
- 详细步骤见下方"执行流程"。

## v2.7 复盘优化（真实判决书实战）

- **裸人名启发式保守化**：两字人名必须带强上下文（向/与/欠/借/付/称/诉等），
  仅靠高频不再判定——修复"华临/余杭/发包人/工程款/司法"等常见词被全文误替的问题；
  候选后接公司/商户/地名尾缀（含 OCR 空格"公 司"）时拒绝。
- **公司简称放开前边界**：全称规则先执行 + 占位符内部防护，`X公司` 在任意位置命中
  （"被告华临公司""支付华临公司"不再漏）。
- **地块编号入规则层**：`XX储出(YYYY)NN号地块` → `[地块编号]`。
- **审阅清单增强**：新增"还原往返校验"（restore 后与原文逐字节一致 ✅/❌）；
  "角色词人名残留"按姓氏/jieba 过滤，减少"原告及其原委"这类词组误报。
- **语义层命令化**：新增 `semantic` 子命令，自动做法院/地块/商户/项目名后缀的
  系统化替换并合并映射表、对照原文校验还原；文档特定实体仍由 AI 补充。

## v2.8 三份训练语料实战修复（2026-08 规则层大修）

基于公开渠道搜集的三份训练语料（舒城民间借贷判决书 / 昌黎行政处罚决定书 /
南沙建设工程合同判决书）跑规则层并逐项修复：

### 1. 还原错位根因修复（地址规则 shift 串用）
- **地址 5 个子模式此前共用同一个 shift**，导致第 2 个子模式之后的替换事件
  位置被重复扣减，回放时误删 `[出生日期]` 占位符 → restore 配对错位。
- 现在每个子模式独立 shift，三份语料"还原往返校验"全部逐字节一致。

### 2. 角色词后姓名吞字/误判修复
- **姓名+动词粘连不再整吞**：`原告彭静娴诉被告张旭`→`诉`保留、
  `被告张旭到庭`→`到`保留、`被告张旭未有提供证据`→`未有`保留。
  新增单字 glue 集（诉/到/有/未/无/签/定…）与尾部回退。
- **非姓氏双字词不再是姓名**：`收到`、`无异议`、`签定` 不再被当人名
  （角色词后候选必须姓氏开头）。
- **量化词前缀**：`原告全部诉讼请求` 不再被当"原告+姓名"。
- **姓氏表扩充**：新增 150+ 常见姓氏（倪/聂/金/田/石/文…），
  并排除 员/应/银/云 等与高频词冲突的姓氏。

### 3. OCR 空格写法支持
- 角色词容忍行内空格：`审 判 员 汪 瑜` → `[法官]`。
- 姓名容忍行内空格：`审判员 倪 平`、`书记员 杨梅红` → 对应占位符，
  还原逐字节保留空格（修复"书记员 杨梅红"还原丢空格）。
- 角色词后允许行尾/换行结尾（`法定代表人：郭振兴` 不再漏）。

### 4. 公司名修剪与角色绑定
- 公司名匹配串整串修剪：`（以下简称宝冶公司）与被告广州合生东宇房地产有限公司`
  → 只替换公司名本身；`扣押或冻结被告…公司`、`均由被告…公司` 不再吞上下文。
- 泛化词拒绝：`该公司`、`原告公司`、`微信告知原告公司` 不再被当公司名。
- **简称↔全称子串链接**：`宝冶公司`↔`上海宝冶集团有限公司`、
  `合生东宇公司`↔`广州合生东宇房地产有限公司` 统一占位符，
  修复同一公司在不同位置出现 4 种占位符的角色绑定错乱。
- 新增商户名规则：`昌黎县嘉瑞丰煎肉店` → `[当事人单位]`
  （行政处罚决定书"当事人/经营者"角色）。

### 5. 证件与地址补强
- 带"身份证号码"标签的 15~17 位残缺号码（如 `3424251967112040X`）整体替换
  为 `[身份证号]`，不再兜底成 `[银行账号]` 并残留 `X`。
- 新增罚没许可证号规则：`罚没许可证号：07040008` → `[罚没许可证号]`
  （不再被金额规则误判），并纳入审阅清单关键信息校验。
- 地址规则补强：`住/现住/户籍地` 前缀、`省→县` 直连、
  `昌黎县四街铁塔东里片` 等无省市区层级地址；移除"庄/组"等过激尾缀
  （避免"帝景山庄一组团"被误当地址）。

### 6. 金额先于裸人名
- 金额 pass 提前到裸人名之前：`伍佰万元整` 不再被"伍"（姓氏）误当人名。

### 7. 回归保障
- 单测 59 → **74**（新增 9 个 v2.8 回归：17 位身份证/粘连动词/OCR 空格/
  量化词/商户与许可号/公司统一占位符/还原错位等）。
- 红队语料 65 → **77**（新增 12 条，含负样本），全量通过、误报 0。
- 三份训练语料 `mask --review`：关键信息 0 残留、还原往返逐字节一致。

## v3.0 内化 rizzo-pii 五大优点（合成语料 + validated + PDF 涂黑 + 还原漂移 + 评测口径）

参照开源项目 [Rizzo-AI-Academy/rizzo-pii](https://github.com/Rizzo-AI-Academy/rizzo-pii.git)
（意大利语 PII 脱敏，LLM 写模板 + 代码注入校验值的合成语料管线），把五个优点内化到本工具：

### 1. 合成语料管线 `synthetic/`（最大亮点：LLM 写模板、代码注入校验值）
- **原则**：LLM 只写法律文书的 prose（民事判决书/行政处罚决定书/合同/调解书/聊天记录…），
  且只能使用白名单占位符 `{当事人甲}` `{身份证}` `{银行账号}`…；真正的号码由
  `generate_synthetic_pii.py` 用**代码**生成数学上合法的值（身份证 GB 11643-1999、
  信用代码 GB 32100-2015、银行卡 Luhn），BIO 标注天然精确、LLM 永不产出真实 PII。
- `synthetic/llm_template_bank.py`：让 LLM（OpenAI 兼容，本地 Ollama 或云端）写模板，
  带 **QA 门控**（find_stray_names）：占位符外若夹带"原告王某某"式姓名 → 整条丢弃。
  不接 LLM 也能用（内置 11 个模板兜底）。
- `python3 synthetic/generate_synthetic_pii.py -n 300` → `测试/合成语料.jsonl`
  （自动填好 expect_masked/expect_kept，直接喂 evaluate.py）；`--bio` 同时输出
  BIO 训练语料。实测 300 条合成语料规则层召回率 99.76%、precision 1.0、误报 0。
- 合成语料驱动修复了 4 个规则层**真实盲区**：角色词"被告人/被申请人/当事人/经营者"
  缺失、"25日在安徽省…"地址吞日期、jieba 把"荣墨军称"切成"荣墨"+"军称"、
  3/4 字人名形态。

### 2. 校验码 strict/lenient 双档 + validated 标记（映射表"验证"列）
- 统一检测器表语义：`strict=True`（形状泛，校验不过即丢弃：无标签身份证）、
  `strict=False`（形状特异/标签权威，校验不过也脱敏，宁替勿漏：裸银行卡、信用代码）。
- 每条映射带 `validated` 算法验证标记，映射表 Markdown 新增**"验证"列**：
  `✓`=校验码通过（GB11643/GB32100/Luhn），`—`=仅格式/标签命中。
  律师一眼看出"哪条是算法验证过的"。`scan` 输出也带 validated/strict。

### 3. PDF 真·涂黑脱敏（保留版式，带 residual 零残留校验）`pdf_redact.py`
- `mask -f 判决书.pdf --pdf-redact -o 判决书_redacted.pdf`（或 -o 指定 .pdf 自动启用）。
  字符级精确匹配（每字符 bbox）、容忍 OCR 空格/换行（"汪 瑜"、"审 判 员"）、
  数字/字母值带词边界；长值先涂 + 已涂矩形跳过，防止"张三"破坏"张三丰"。
- 连带清理元数据/XMP、批注、表单字段、书签、内嵌附件。
- **安全语义**：输出后 residual 校验（原文仍可读 → 拒绝交付）；整份 PDF 零命中
  （扫描件文字在图片里）→ 报错，绝不交付"假脱敏"PDF；过短值跳过并警告。

### 4. 还原容忍 markdown 漂移（`restore_text`）
- 占位符按键长倒序 + 正则 `\**\[?\s*占位符\s*\]?\**`：AI 回复把
  `[当事人甲（原告）]` 改成 `**当事人甲（原告）**`、丢括号、加多余空格都能无损还原。
- 先精确（带括号）后宽松两遍防"语义 inner 词撞上下文前缀"（"总金额[金额]"不误吞）；
  精确/漂移混用时按原文顺序配对，空格不丢失。

### 5. 评测口径升级：entity-level per-tag P/R/F1 + support（`evaluate.py`）
- 正样本=expect_masked（命中=TP、漏=FN）；**负样本=expect_kept（误替换=FP）**。
- 分类型 precision/recall/F1 + support，外加 MICRO（总体）与 MACRO（类型平均）——
  规则层最怕误伤，precision/F1 才是"改保守了还是改漏了"的量化指标。
- 每次改规则后：`python3 evaluate.py`（原语料）+ `python3 evaluate.py -c 测试/合成语料.jsonl`
  （合成语料）双基线对比。

### 验证方法（v3.0 起）
```bash
python3 -m unittest test_desensitize     # 94 个单测（v2.8 74 + v3.0 20）
python3 evaluate.py                      # 77 条红队用例（100% 召回、误报 0）
python3 synthetic/generate_synthetic_pii.py -n 120
python3 evaluate.py -c 测试/合成语料.jsonl   # 合成语料基线（99%+ 召回、误报 0）
python3 desensitize.py mask -f 判决书.pdf --pdf-redact -o 判决书_redacted.pdf
```

### v3.1 修复（微信号 + 姓名动词尾，见 docs/版本说明.txt）
- 微信号脱敏增强：词表加"微信号码"、容忍 OCR 空格（"微 信号"）、分隔符支持
  "：: 为 是 ="（微信号是xxx / 微信号为xxx / 我的微信是xxx 均脱敏）。
- 姓名+高频动词尾误报防护："张三确认/双方确认/什么时候" 不再被拆成人名；
  单测 104、红队 84 条（新增 wx_05~09 + neg_10/11）、误报 0。

### v3.2 修复（手机号式微信号，见 docs/版本说明.txt）
- 微信号就是手机号是常见场景：纯 11 位手机号由手机号规则兜底（[手机号]）；
  带分隔符（138-0013-8000）与 +86/86 前缀（+8613800138000）由微信号规则补充
  为 [微信号]，且不再被金额规则误标。单测 110、红队 89 条、误报 0。

## v3.3 Excel（.xlsx / .xlsm）支持（银行流水 / 证据清单 / 对账单）

- **单元格级脱敏**：每个参与脱敏的单元格展平为一行文本交给规则层（同一份文本
  整体脱敏，EntityResolver 全文一致仍生效：公司全称/简称跨单元格统一占位符）。
- **保留**：工作表结构、样式、列宽、合并单元格、公式（`=SUM(...)` 原样不动，
  公式是结构不是内容）、日期时间与布尔单元格（不参与脱敏，保持原格式）。
- **写回**：仅改动被脱敏的单元格；未改动单元格保持原值（数值/日期类型与格式
  原样保留）。
- **restore 逐单元格还原**：restore 以脱敏文件为基底时，按映射表恢复数值类型
  （金额等 → int/float）；身份证/银行卡/手机号等号码类即使全数字也保持文本。
- 依赖：`pip3 install openpyxl`（缺失时优雅退出并提示，不影响其他格式）。
- 验证：`python3 -m unittest test_desensitize`（120 项，含 TestExcelSupport 10 用例）
  + `python3 evaluate.py` 红队回归（105 项 MICRO/MACRO 全 1.0000、误报 0）
  + CLI 端到端冒烟（银行流水 xlsx：mask → restore 往返逐单元格一致，含类型）。

### 验证方法（v3.3 起）
```bash
python3 -m unittest test_desensitize     # 120 个单测（含 Excel 10 用例）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
python3 selfcheck.py                     # 自检：文件/测试/评测/还原四项
python3 desensitize.py mask -f 银行流水.xlsx --save-mapping 映射表.md
python3 desensitize.py restore -f 银行流水_desensitized.xlsx -m 映射表.md -o 还原.xlsx
# 还原后逐单元格与原文件比对，要求 0 差异（含类型）
```

## v3.4 修复（支付平台对手方 / 扫描件 PDF / 假 PDF）

### 1. 支付平台前缀交易对手（银行流水高频格式）
- "支付宝-刘方立" / "微信转账-张三" / "财付通：李四" / "支付宝 王五" 等
  "平台词 + 分隔符 + 人名" 结构 → 平台名保留（公开品牌不脱敏）、人名替换为
  `[当事人_N]`（不同对手不同编号、同一对手全文一致占位符）。
- 识别约束（避免误伤）：强制要求分隔符 ≥1 个（"微信支付""支付宝到账"等
  无分隔符普通短语不匹配）；候选须以常见姓氏开头并通过姓名 plausibility 校验。
- 平台词（支付宝/微信/财付通/云闪付/银联/翼支付/京东支付/快钱/拉卡拉/平安付/
  易宝支付/PayPal/贝宝/手机银行/网上银行/网银/掌上银行/Apple Pay/微信转账/
  微信红包/支付宝转账/支付宝红包）同步加入裸人名黑名单，防止"支付宝"被当人名。
- 归因：v2.7 收紧裸人名（宁漏勿误）导致"平台名+人名"低频格式漏识别。

### 2. 扫描件 PDF 明确报错（不再静默产出空文件）
- `read_text_from_file` 的 PDF 分支：整份提取不到任何文本 → `sys.exit` 报错
  "未从 PDF 提取到任何文本层（疑似纯图片扫描件）"并给出 OCR 指引
  （ocrmypdf / WPS / Adobe "识别文本"）。之前会静默产出 0 字节假成功文件。

### 3. PDF 输入自动命名 .txt（不再产生假 .pdf）
- mask / full / semantic 对 PDF 输入的自动输出扩展名改为 `.txt`
  （与 SKILL.md 声明一致）。之前沿用输入扩展名，产出"文本内容 + .pdf 后缀"
  的假 PDF。`--pdf-redact` 仍自动输出 `_redacted.pdf`。

### 验证方法（v3.4 起）
```bash
python3 -m unittest test_desensitize     # 127 个单测（v3.3 120 + v3.4 7）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
python3 selfcheck.py                     # 自检：6 项必检全部通过
printf '支付宝-刘方立\n微信转账-张三\n' | python3 desensitize.py mask
# → 支付宝-[当事人_1] / 微信转账-[当事人_2]
python3 desensitize.py mask -f 扫描件.pdf   # → 明确报错 + OCR 指引
python3 desensitize.py mask -f 判决书.pdf   # → 判决书_desensitized.txt（不再假 .pdf）
```

## v3.5 银行流水场景升级（借鉴第三方框架）

参考一个"银行流水脱敏引擎 v2.0"框架，用其真实 demo 数据（5 行银行流水）
量化差距后，新增/修复 4 项：

### 1. 账号/户名组合（对方账号与户名列高频格式）
- `6212261001014270893/胡若薇` → `[银行账号]/[当事人_1]`（账号+户名都要脱敏，
  之前裸人名启发式因 count<2 漏掉户名"胡若薇"）
- `7399/支***刘方立` → `[支付宝账号][当事人_1]`（支付宝掩码账号）
- 兼容 `_mask_bank_card` 已把账号替换为 `[银行账号]` 占位符的场景
  （此时只补户名，不重复记录账号事件）

### 2. 银行分支机构名（交易地点/附言列）
- `中国建设银行股份有限公司安徽省分行本级本币头寸机构` → `[公司_1][银行机构]`
  （公司名规则只覆盖"…有限公司"，机构尾缀补 分行/支行/本级/本币/头寸/机构）
- `工商银行北京长安支行` → `[银行机构]`；"该分行表示同意"不误伤

### 3. 交易日期不再被金额规则误标
- 金额大数规则排除 8 位日期（`20181009` 是流水交易日期，不是金额；
  之前被"7 位以上无单位大数"误标 [金额]）

### 4. restore 掩码账号星号无损还原
- `7399/支***` 还原时，占位符正则的 `\**`（容忍 AI 加粗）会把原文掩码 `***`
  吞掉。修复：单侧星号视为原文内容保留；成对 `**...**` 才是 AI 加粗丢弃。

### 验证方法（v3.5 起）
```bash
python3 -m unittest test_desensitize     # 134 个单测（v3.4 127 + v3.5 7）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
python3 selfcheck.py                     # 自检：6 项必检全部通过
# 银行流水场景（第三方框架 demo 5 行全部通过，restore 逐字节一致）
printf '6212261001014270893/胡若薇\n7399/支***刘方立\n' | python3 desensitize.py mask
# → [银行账号]/[当事人_1] / [支付宝账号][当事人_1]
```

## v3.6 列感知脱敏（银行流水表格，mask --table-aware）

### 目标
律师将客户银行流水（Excel / 带文本层 PDF）直接上传处理，审计报告确认
无明显个人信息泄露。v3.5 后仍有 3 个硬伤：孤立中文姓名（对方户名列只有
"胡若薇"一个值，count=1 漏掉）、字段含义（附言列 12 位联行号被误标金额）、
审阅清单查不出孤立姓名残留。v3.6 全部解决。

### 1. 结构化读取（read_structured_table）
- .xlsx：openpyxl 读行，首行作表头候选
- .pdf：PyMuPDF `find_tables(strategy='text')` 提取表格（电子银行导出常见）
- 仅当表头含 ≥2 个已知列类型关键词才返回结构化，否则 None → 自动回退普通文本

### 2. 列类型识别（_classify_column）
表头列名 → 列类型（优先级从高到低）：
- 户名：对方账号与户名/对方户名/交易对手/收款人/付款人…
- 日期：交易日期/记账日期/业务日期
- 金额：交易金额/账户余额/收入/支出…
- 附言：附言/摘要/备注/用途

### 3. 列感知脱敏（mask_table / _mask_cell）
- 每格独立走规则链，共享 EntityResolver → 同一对手全表统一占位符
- 户名列：孤立中文姓名（2~4 字、姓氏开头、整格无其他内容）强制识别
- 日期列：不脱敏（业务信息）
- 附言列：`_run_rules_no_amount` 跳过金额 pass（12 位联行号/交易代码不误标）
- 事件坐标：`_event_offset` 记录每格全文偏移，restore 逐单元格无损还原（含类型）

### 4. 审计清单增强（要求 8）
- `scan_remaining_risk` 新增"孤立姓名候选"：普通模式下漏掉的孤立姓名
  （胡若薇/徐常英）会在审阅清单显式提示，不再静默"0 残留"
- 金额残留跳过 12~13 位非 19/20 开头数字（联行号不误报）
- 排除财务/表头词（消费/转账/序号/附言…）避免误报

### 验证方法（v3.6 起）
```bash
python3 -m unittest test_desensitize     # 141 个单测（v3.5 134 + v3.6 7）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
python3 selfcheck.py                     # 自检：6 项必检全部通过
# 律师工作流：一行命令 + 审计报告
python3 desensitize.py mask -f 银行流水.xlsx --table-aware --review \
  --save-mapping 映射表.md
# → 银行流水_desensitized.xlsx + 审阅清单（关键信息 0 残留 + 还原往返校验）
# 孤立姓名"胡若薇"在户名列被识别为 [当事人_N]（count=1 也识别）
```

## v3.7 扫描件 PDF 内置 OCR（macOS Vision，无需安装工具）

### 背景
v3.6 前扫描件 PDF（无文本层）只能"明确报错 + 提示外部 OCR"。实测本机
macOS Vision 框架中文 OCR 完全可用（无需 tesseract/ocrmypdf），故内置。

### 实现
- `ocr_vision.swift`：macOS Vision 框架中文/英文 OCR（串行批量、页码分隔、
  文件名排序保证页序），随工具分发
- `_ocr_pdf_with_vision()`：PyMuPDF 把每页 200DPI 渲染为 PNG → 首次
  `swiftc` 编译缓存二进制 → 批量 OCR → 拼接文本（页间 PAGE 分隔）
- `read_text_from_file` PDF 分支：无文本层时先内置 OCR，成功则继续脱敏；
  失败（非 macOS/无 Vision/OCR 空）才明确报错并给外部 OCR 指引

### 使用
```bash
# 扫描件直接上传处理（macOS 自动 OCR）
python3 desensitize.py mask -f 银行流水扫描件.pdf --table-aware --review
# Windows/Linux：仍提示装外部 OCR（ocrmypdf/WPS/Adobe"识别文本"）
```

### 已知边界
- OCR 质量取决于扫描清晰度（建议 ≥300 DPI 扫描）；密集表格排版（小字号、
  无空格汉字串）识别可能有误差——这是所有 OCR 引擎的固有局限，审阅清单
  的"孤立姓名候选"等残留检查会兜底提示
- 内置 OCR 仅 macOS（Vision 框架）；其他平台回退明确报错
- 首次运行需编译 ocr_vision.swift（约 30~60 秒），之后复用缓存二进制

### 验证方法（v3.7 起）
```bash
python3 -m unittest test_desensitize     # 144 个单测（v3.6 141 + v3.7 3）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
python3 selfcheck.py                     # 自检：6 项必检全部通过
# 真扫描件（无文本层）端到端
python3 desensitize.py mask -f 扫描件.pdf --review
# → 自动 OCR → 脱敏 txt + 审阅清单（关键信息 0 残留 + 还原往返一致）
```

## v3.8 图片输入支持 + 银行流水 OCR 场景规则修复

### 1. 图片文件直接脱敏
- `read_text_from_file` 新增 .png/.jpg/.jpeg/.bmp/.webp/.tif 分支：
  macOS Vision OCR 识别 → 直接走脱敏流程（复用 v3.7 的 ocr_vision.swift）
- 图片输入自动命名输出 `.txt`（同 PDF，避免"假图片"文本文件）
- 实测：真实建行流水截图（客户徐常英）→ 卡号/人名/公司/账号全部识别并脱敏

### 2. 银行流水 OCR 场景规则修复（用真实截图实测驱动）
- **支付宝外部商户-人名**：商户为个人时（支付宝外部商户-施明月/徐丹丹/
  张在芳），补平台词"支付宝外部商户"等；"个体工商户"修饰前缀也支持
- **姓名含"在"字**（张在芳/李在明）：`_ROLE_NAME_REJECT` 虚词表曾误伤，
  改为仅拒绝"首字虚词"或"虚词在末尾且内部有虚词"的组合
- **公司名含地域括号**："飒拉商业（上海）有限公司" 曾被 `_trim_company_span`
  误当简称括号切坏；右锚定切分不再在括号处切
- **占位符后姓名**："[银行账号]/谢林轩" 后换行出现另一个 "[银行账号]/" 时，
  占位符标签"银行"被误判机构后缀；机构后缀检查跳过占位符

### 3. 已知边界
- OCR 换行会打断跨行公司名（"迅销（中国）商贸⏎有限公司"）——规则层不跨行
  匹配（有意设计，docx 逐段对齐）；审阅清单的"公司/机构简称残留"会兜底提示
- OCR 错字（`支付案`=支付宝、`I12`=6212、`報安耀`）无法规则层覆盖，
  审阅清单"孤立姓名候选"会提示

### 验证方法（v3.8 起）
```bash
python3 -m unittest test_desensitize     # 150 个单测（v3.7 144 + v3.8 6）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
python3 selfcheck.py                     # 自检：6 项必检全部通过
# 图片直接脱敏（macOS）
python3 desensitize.py mask -f 银行流水截图.png --review --save-mapping 映射表.md
# → 银行流水截图_desensitized.txt + 审阅清单
```

## v3.9 原图涂黑脱敏（mask --image-redact）

### 背景
用户问"为什么不能直接在原图上脱敏然后生成脱敏后的原图 PDF"——技术上完全
可行：macOS Vision OCR 能返回每个词的坐标框（bounding box）。此前图片输入
只走"OCR→文本→txt"，丢失坐标信息。v3.9 补齐：直接在**原图上涂黑**敏感区域，
保留原图版式，输出 PDF。

### 实现
- `ocr_vision_boxes.swift`：Vision OCR 输出 JSON（text + 归一化 bounding box
  坐标 x/y/w/h，原点左下），随工具分发
- `image_redact.py`：图片 → 带坐标 OCR → 用 mask 映射表定位敏感值坐标框 →
  在原图对应像素区域画黑矩形 → 输出 PDF；**residual 零残留校验**（涂黑后
  重新 OCR，原文不可读才交付）；零命中拒绝交付"假脱敏"
- `mask --image-redact`（或 -o 指定 .pdf 自动启用）

### 实测（用户真实建行流水截图）
- 76 处涂黑，residual 零残留校验通过
- 复查：卡号 6214661710011477 / 客户名 徐常英 / 施明月 / 账号 / 公司名
  全部不可读；"卡号/账号"前缀、"交易明细"标题等版式内容保留
- 输出 PDF 与原图同尺寸（981×1325）

### 使用
```bash
# 图片直接输出原图涂黑 PDF（保留版式）
python3 desensitize.py mask -f 银行流水截图.png --image-redact -o 脱敏.pdf
```

### 已知边界
- 仅 macOS（Vision 带坐标 OCR）；Windows/Linux 需外部 OCR
- 涂黑精度依赖 OCR 坐标质量（低清图/密集排版可能定位偏差）
- OCR 错字（如 I12=6212）无法匹配定位 → 该值留在 not_found 提示中，
  需人工/语义层处理

### v3.9.1 修复（实测发现）
- **两次 OCR 不一致漏涂**：初版用"首次文本 OCR"生成映射、再用"坐标 OCR"
  定位，两次识别错字不同（迅销/戴豆寵/7227 等漏涂）。修复：改用
  **坐标 OCR 的同一份文本**跑规则层（`_ocr_boxes_text` 行聚合重建），
  映射与坐标天然一致，40/40 全部命中
- **Vision OCR 补全猜测**：黑色矩形遮挡后 Vision 仍能根据上下文"猜"出
  被盖文字（residual OCR 复查误报）。修复：residual 改为**双层校验**——
  像素级（涂黑矩形中心确为纯黑，可靠判定）+ OCR 级（补全猜测仅提示）
- 实测：76 处涂黑、覆盖页面 25.4%（对方账号与户名列区域），迅销/卡号/
  戴豆寵等关键区像素全黑确认

### 验证方法（v3.9 起）
```bash
python3 -m unittest test_desensitize     # 153 个单测（v3.8 150 + v3.9 3）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
python3 selfcheck.py                     # 自检：6 项必检全部通过
python3 desensitize.py mask -f 截图.png --image-redact -o 脱敏.pdf
# → 原图涂黑 PDF（保留版式 + residual 校验）
```

## v3.10 扫描件 PDF → 涂黑 PDF（PDF 输入输出全支持）

### 背景
用户问"PDF 脱敏后能输出 PDF 吗？只是原图涂黑吗"。实测厘清三种路径：
- 电子版 PDF（有文本层）→ `--pdf-redact` 字符级涂黑 → PDF ✅（v3.0）
- 扫描件 PDF（纯图片）→ 此前只输出 txt（OCR 后），缺涂黑 PDF ← v3.10 补齐
- 图片 → `--image-redact` 原图涂黑 → PDF ✅（v3.9）

### 实现
- `image_redact.py` 重构：`_redact_pages` 公共核心（多页），
  `redact_scanned_pdf`（PDF 每页 200DPI 渲染 → 逐页涂黑 → 输出多页 PDF）
- `_ocr_boxes` / `_ocr_boxes_text` 支持多图列表（一次 OCR 跨全部页，page 字段区分）
- CLI 自动分流：`--image-redact` 对无文本层 PDF 走 redact_scanned_pdf；
  `--pdf-redact` 对电子版 PDF 走字符级涂黑；互不干扰

### 绕过的 PyMuPDF 1.26.5 坑（实测定位）
- 先创建多页、后引用前面页 → page.parent 失效（draw_rect 报 doc is None）
  → 逐页"创建→插图→Shape画矩形"一步完成
- 插图后 draw_rect 报错；先画矩形会被图片盖住 → 插图后用 Shape API
- get_drawings() 把 Shape 画的多个矩形合并成一个 Path（包围盒超宽）
  → residual 校验改用自己收集的精确矩形 + 多点采样

### 实测
- 多页扫描件（2 页）：每页正确涂黑，OCR 复查敏感值不可读
- 用户图片 2 页 PDF 模拟：324 处涂黑，residual 像素全黑
- 单页图片（v3.9 回归）：74 处涂黑通过；电子版 --pdf-redact 回归正常
- 修复规则层误伤：温馨/连续/便捷/快捷 加入裸人名黑名单
  （流水页脚"温馨提示/连续交易"不再被当人名）

### 验证方法（v3.10 起）
```bash
python3 -m unittest test_desensitize     # 181 个单测（v5.0 起全量）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
python3 selfcheck.py                     # 自检：6 项必检全部通过
# 扫描件 PDF → 涂黑 PDF
python3 desensitize.py mask -f 银行流水扫描件.pdf --image-redact -o 脱敏.pdf
# 电子版 PDF → 字符级涂黑 PDF
python3 desensitize.py mask -f 判决书.pdf --pdf-redact -o 脱敏.pdf
```

## v4.0 批量模式（借鉴本地卷宗处理工作流）

把"整卷宗批量处理"纳入 CLI：`mask --batch <文件夹>` 递归处理文件夹内所有
支持格式（.txt/.docx/.pdf/.xlsx/.xlsm/图片），逐文件执行与单文件完全相同的
脱敏全流程，并补齐四件律师最关心的事：

### 1. 断点续跑（--resume）
- 每个文件处理完成后立即写入 checkpoint（`.desensitize_checkpoint.json`），
  中断后 `--resume` 只处理未完成的文件，不重复劳动；
- checkpoint 只记录路径/状态/耗时/替换数/原件指纹，**不保存正文**。

### 2. 批量处理报告
- 自动生成 `批量脱敏报告.txt`：逐文件结果（状态/耗时/替换数/输出/审阅清单）、
  原件校验、需人工复核文件清单、数据流审计；
- 报告分级：🔴 重点复核（关键信息残留/处理失败）→ 🟡 建议复核 → 其余抽查。

### 3. 原件不变校验
- 处理前后对每个原件记录 大小 + 修改时间 + sha256 指纹，报告输出
  "✅ 全部原件未被修改" 或列出异常文件；
- 工具从不修改原件，但把"校验证据"写进报告，供律师放心使用。

### 4. 中途记录清理（--clean-temp）
- 处理完成后清理 OCR 编译缓存（legal_deid_ocr_vision*）、临时渲染目录
  （deid_ocr_*/deid_pdf_*）和断点文件；不影响最终脱敏件/映射表/审阅清单。

### 用法
```bash
# 批量脱敏整个卷宗文件夹（推荐先 --review 出审阅清单）
python3 desensitize.py mask --batch ./卷宗 --review
# 中断后续跑（跳过已完成文件）
python3 desensitize.py mask --batch ./卷宗 --review --resume
# 输出集中到独立目录（推荐，避免与输入混放），结束清理 OCR 中途记录
python3 desensitize.py mask --batch ./卷宗 --review \
  --output-dir ./脱敏输出 --clean-temp
# 批量映射表加密保存
export DESENSITIZER_MAPPING_PASSWORD="your-password"
python3 desensitize.py mask --batch ./卷宗 --review \
  --output-dir ./脱敏输出 --encrypt-mapping
```

### 审阅清单升级（复核分级）
- 🔴 重点复核：关键信息残留（身份证/手机号/银行卡/案号/信用代码/执业证号等），
  逐条列出，处理前请勿分享/上传；
- 🟡 建议复核：低优先级残留（法院名/公司简称/地址/项目名/孤立姓名等）；
- 报告/清单均注明："复核分级只用于安排检查顺序，不能证明文档已安全；
  关键信息校验 ✅ 仅表示规则层已覆盖，正式引用/上传前仍需人工抽检原件。"

### 批量模式安全要点
- 批量输入文件夹内已有的 `_desensitized` / `_redacted` / `_审阅` / `_语义层`
  输出文件会被自动跳过，不会二次处理；
- 建议用 `--output-dir` 把脱敏件与原件分目录存放，原件目录保持干净；
- 批量报告会列出"需人工复核文件"，首次跑大批量前先拿 1–2 份代表性文件
  （含表格/手写）试跑确认，再分批全量。

### 验证方法（v4.0 起）
```bash
python3 -m unittest test_desensitize     # 181 个单测（含批量/断点/报告/分级/v5.0）
python3 evaluate.py                      # 红队用例（105 项命中、误报 0）
# 端到端冒烟
mkdir -p /tmp/卷宗 && printf '原告陈建国，身份证号110101198001011232。\n' > /tmp/卷宗/a.txt
python3 desensitize.py mask --batch /tmp/卷宗 --review --output-dir /tmp/脱敏输出
```
