# 开发与发布

> 与脱敏业务无关的仓库维护流程（提交、推送、发布、测试）集中在这里，
> SKILL.md 只保留一行指引。完整版本更新记录见 [`CHANGELOG.md`](CHANGELOG.md)。

## 代码提交

```bash
cd /Users/lirunyi/Downloads/法律文书脱敏工具/legal-document-desensitizer
git add -A && git commit -m "feat/fix: 描述"
```

## 推送到 GitHub（本机网络受限时的标准方式）

本机 `git push` 直连 github.com:443 常被网络阻断，**统一改用 GitHub REST API
推送**（api.github.com 可通过 curl 访问）：

```bash
# 1. 本地正常提交
git add -A && git commit -m "feat: xxx"

# 2. 用 API 推送（推荐加 --sync-local：推送后本地 main 与远程 SHA 完全一致）
python3 git-push-api.py --sync-local

# 3. 预览不推送
python3 git-push-api.py -n
```

`git-push-api.py` 会把本地领先的提交通过 GitHub Git API 逐个重建到远程，
blob 与本地 `git hash-object` 逐字节核对，并在 `--sync-local` 时把远程提交
对象重建回本地（消息换行 × 时区偏移组合还原 SHA），保证本地与远程历史完全一致。
Token 自动读取 `~/.config/gh/hosts.yml`（gh CLI），也支持 `GITHUB_PAT_TOKEN`
环境变量或 `--token` 参数。

备用脚本 `api-push.py`：以远程分支最新树为基础树，按「上传文件 → 建树 →
建提交 → 更新分支」推送指定文件（新增 / 修改 / 删除），同样带
`--sync-local` 选项。两者都只依赖 Python 标准库。

## SKILL.md 本地副本同步规则

`~/.codex/skills/legal-document-desensitization/SKILL.md`（已安装副本目录）
由项目 `SKILL.md` 全量同步而来；v5.3 起 frontmatter `name` 统一为
`legal-document-desensitizer`（与项目目录名一致）。常规升级只推送到 GitHub，
**不**主动更新本地副本；仅在用户明确指示"更新本地 skill"时，才重新全量同步：

```bash
cp SKILL.md ~/.codex/skills/legal-document-desensitization/SKILL.md
```

## 全量测试与红队评测

```bash
# 单元回归（v5.3 全量 217 项）
python3 -m unittest test_desensitize test_llm_layer -v

# 红队评测（测试/红队语料库.jsonl，保持 100% 召回、0 误报）
python3 evaluate.py

# 冒烟：单文件 mask --review
python3 desensitize.py mask -f 测试/01_民事判决书.txt --review
```
