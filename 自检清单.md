# 法律文书脱敏工具 — 自检清单

> 本清单同时面向**人和 AI 代理**。AI 代理拿到本文件后，请按"执行步骤"逐条执行命令，
> 依据"预期结果"判定 PASS/FAIL，最后汇报结果与任何 FAIL 项。

## 目的

验证一份拷贝（clone/安装）与 GitHub 官方版本**行为一致、可复现**：

- 规则层（mask/scan/restore/校验码/评测）是确定性代码：文件一致 + 测试一致 + 评测一致 + 还原一致，即证明拷贝无误
- LLM 层效果取决于本地模型，不承诺一致，但必须**可验证**（见步骤 4）

## 前置条件

- Python ≥ 3.9（命令行执行 `python3 --version` 确认）
- 已获取代码副本：`git clone https://github.com/Lirunyi-123/legal-document-desensitizer`
  （或解压官方分发的完整目录，**必须包含全部文件，不能只拷 SKILL.md**）
- 可选依赖（缺少时相关功能不可用，但不影响规则层判定）：
  `pip3 install cryptography python-docx PyMuPDF`
- 如需 LLM 层检查：已安装并启动 Ollama（`ollama pull qwen2.5`）

## 执行步骤

### 步骤 0：进入仓库目录

```bash
cd legal-document-desensitizer
```

判定：目录下能看到 `desensitize.py`、`evaluate.py`、`llm_layer.py`、
`selfcheck.py`、`SKILL.md`、`测试/红队语料库.jsonl` 等文件。

### 步骤 1：一键自检（核心）

```bash
python3 selfcheck.py
```

预期结果（判定标准）：

| 检查项 | 预期 |
|--------|------|
| 必要文件齐全 | PASS |
| 模块导入 | PASS |
| 单元测试 | PASS（44 项测试 OK） |
| 红队评测 | PASS（0 失败，结构化召回率 100.0%） |
| mask→restore 往返 | PASS（往返一致） |
| 本地LLM二轮脱敏 | SKIP（未装 LLM 属正常）；若装了就应 PASS |
| 可选依赖 | PASS 或 SKIP（WARN 可接受） |

**全部必检项 PASS、退出码 0** → 规则层拷贝验证通过。
有任何 FAIL → 按下方"常见失败排查"处理，处理完重跑，直到全 PASS。

### 步骤 2（可选但推荐）：文件 SHA 比对

```bash
git rev-parse HEAD
git status --short
```

预期：HEAD 与官方仓库 main 分支一致（当前 `acd807f` 或更新的提交）；
`git status` 干净（无本地改动）。有未提交改动说明该副本被改过，需说明改动内容。

### 步骤 3（可选）：不联网抽查还原

```bash
printf '原告：陈建国，身份证号110101198001011232。尾款80万元。\n' | python3 desensitize.py mask --mapping
```

预期：输出映射表含 `陈建国 → [当事人甲（原告）]`、`80万元 → [金额]`、
`110101198001011232 → [身份证号]`。

### 步骤 4（LLM 层验证，需要本地模型）

```bash
# 启动 Ollama 并拉模型（一次性）
ollama serve &  ollama pull qwen2.5

# 用 selfcheck 强制检查 LLM 项
SELFCHECK_LLM_MODEL=qwen2.5 python3 selfcheck.py --llm

# 或用完整评测量化 LLM 层覆盖率
python3 evaluate.py --llm-api ollama --llm-model qwen2.5
```

预期：
- selfcheck 的"本地LLM二轮脱敏"为 PASS（裸人名被替换）
- evaluate 报告 `LLM 层覆盖项（已计入召回率）: 6/6`

注意：LLM 层结果**取决于模型**（qwen2.5 / qwen3 / 其他模型各有差异），
此项不承诺跨模型一致，只承诺"同一模型下可复现、可度量"。

### 步骤 4b（无本地部署：用云端 API 验证 LLM 层）

不想装 Ollama 时，用任意 OpenAI 兼容云端 API 即可（机器零部署）：

```bash
export LLM_API_KEY="你的Key"
SELFCHECK_LLM_API=openai \
SELFCHECK_LLM_ENDPOINT=https://dashscope.aliyuncs.com/compatible-mode \
SELFCHECK_LLM_MODEL=qwen-plus \
python3 selfcheck.py --llm
```

或用完整评测量化：

```bash
python3 evaluate.py --llm-api openai --llm-model qwen-plus \
  --llm-endpoint https://dashscope.aliyuncs.com/compatible-mode
```

预期：`LLM 层覆盖项（已计入召回率）: 6/6`。
⚠️ 云端方案会把规则层处理后的文本（人名/地址/案情细节）发送给 API 服务商，
结构化号码已在规则层被替换；涉密材料请改用本地 Ollama。

## 判定汇总

- 步骤 0~3 全过 → **规则层与本仓库完全一致**，可放心使用
- 步骤 4 由实际模型决定，达标线是 LLM 覆盖项 6/6
- 任何 FAIL 都不应被忽略：要么修复，要么明确告知维护者

## 常见失败排查

| 现象 | 原因与处理 |
|------|-----------|
| `ModuleNotFoundError: desensitize` | 没在仓库目录里跑；`cd` 进目录再执行 |
| 单元测试 FAIL | 文件被改动/拷贝不完整；重新 clone 后重跑 |
| 评测失败：>0 失败或召回率 <100% | 语料库或代码版本不一致；对比 `git rev-parse HEAD` |
| `import cryptography` 报错 | `pip3 install cryptography`（只影响映射表加密） |
| LLM 项 FAIL：无法连接 | 未启动 Ollama；`ollama serve` 或确认 `SELFCHECK_LLM_ENDPOINT` 地址 |
| LLM 项 FAIL：模型未拉取 | `ollama pull qwen2.5`；模型名写错则设 `SELFCHECK_LLM_MODEL` |
| 云端 API 401/403 | Key 错误或未设置；`export LLM_API_KEY=...` 或 `--llm-api-key` |
| LLM 输出被拒绝（行数/占位符/残留） | 属失败安全机制正常工作；换更强的模型或调整提示词 |

## 给 AI 代理的汇报格式

完成自检后，请输出：

1. `python3 selfcheck.py` 的逐项结果（PASS/FAIL/SKIP）
2. `git rev-parse HEAD` 与 `git status --short` 结果
3. 如有 FAIL：失败项原文 + 你采取的排查动作与结果
4. 结论：规则层"通过/不通过"；LLM 层"未测/6/6/其他"
